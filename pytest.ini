[pytest]
testpaths = tests
python_files = test_*.py
python_classes = # No classes, using functions only
python_functions = test_*
addopts = 
    -v
    --tb=short
    --strict-markers
markers =
    integration: Integration tests that build real ISOs
    slow: Tests that take a long time to run
